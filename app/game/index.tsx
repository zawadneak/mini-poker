import { View, Text } from "react-native";
import React from "react";

type Props = {};

const Game = (props: Props) => {
  return (
    <View>
      <Text>Game</Text>
    </View>
  );
};

export default Game;
